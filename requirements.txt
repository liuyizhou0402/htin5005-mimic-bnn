# CPU only -- no GPU is used or needed.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tensorflow>=2.15        # the four Bayesian models; the authors' code is PyTorch
matplotlib>=3.7
pymupdf>=1.23           # only for re-extracting docs/paper_text.txt

# Optional, for bonus B1 by name rather than by algorithm.  Both need libomp
# on macOS (`brew install libomp`); scikit-learn's HistGradientBoostingClassifier
# is used instead by default and implements the same histogram-based GBDT.
# xgboost>=2.0
# lightgbm>=4.0
